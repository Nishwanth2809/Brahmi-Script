# For local development only — leave empty to use the Vite proxy (vite.config.ts).
# In production, Vercel rewrites /api/* to the Render backend automatically.
# VITE_API_BASE_URL=https://brahmi-script.onrender.com
