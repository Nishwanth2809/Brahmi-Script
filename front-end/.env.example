# For local development — leave empty to use the Vite proxy (vite.config.ts).
# In production, set this in Vercel's Environment Variables dashboard.
VITE_API_BASE_URL=https://brahmi-script.onrender.com
